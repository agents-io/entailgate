{
  "name": "@agents-io/entailgate",
  "version": "0.1.1",
  "private": true,
  "description": "Fail-closed evidence gate for AI claims and actions",
  "type": "module",
  "bin": {
    "entailgate": "./dist/src/cli.js",
    "ebr": "./dist/src/cli.js"
  },
  "files": [
    "dist",
    "schemas",
    "spec"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "check": "npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "22.15.3",
    "typescript": "5.8.3"
  }
}
